package ${package.Service};

import ${package.Entity}.${entity};
#if(!$!{cfg.isCreateExt})
import ${package.Entity}.bo.QueryBO;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
#end
import ${superServiceClassPackage};

import java.util.List;

/**
 * <p>
 * 功能描述:$!{table.comment}服务类
 * </p>
 *
 * @author ${author}
 * @version 1.0
 * @since ${date}
 */
#if(${kotlin})
interface ${table.serviceName} : ${superServiceClass}<${entity}>
#else
public interface ${table.serviceName} extends ${superServiceClass}<${entity}> {
    ##不需要扩展
#if(!$!{cfg.isCreateExt})

    /**
     * 根据条件获取分页查询数据
     *
     * @param queryBO 查询条件
     * @return 分页结果集
     */
    Page<${entity}> list${cfg.get(${entity})}ByPageWithParams(QueryBO<${entity}> queryBO);

    /**
     * 根据条件获取分页查询数据
     *
     * @param queryBO 查询条件
     * @return 查询结果集
     */
    List<${entity}> list${cfg.get(${entity})}WithParams(QueryBO<${entity}> queryBO);
#end
}
#end
